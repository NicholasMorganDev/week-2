//Create a function that takes in a number of two-pointers (shots made that count as 2 points each) and 
//three-pointers (shots made that count as 3 points each) made and returns a basketball team's total score. 
//Invoke the function and console.log the results.


function basketball (two, three) {
  const shots1= two * 2
  const shots2 = three * 3
  return score = shots1 + shots2
}

basketball(7, 4)
console.log(score)
